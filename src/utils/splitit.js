


class Splitit {
  constructor(web3, addresses) {
    this.web3 = web3
  }

  initPublish = () => {
    return new Promise(async (resolve, reject) => {
      const { web3, addresses } = this

    })
  }

  find
}

export default Splitit
